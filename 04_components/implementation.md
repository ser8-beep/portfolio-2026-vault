## Implementation Notes

Framework

React

TypeScript

Tailwind

Framer Motion

Dependencies

Avoid inline styles.

Use semantic HTML first.

Forward refs where appropriate.

Use CSS variables mapped to tokens.

Never hardcode colors.

Never hardcode spacing.

Never hardcode typography.

Prefer composition over prop explosion.

