# System Architecture

## Tech Stack
- **Framework**: [e.g., Next.js / Astro]
- **Styling**: [e.g., Tailwind CSS / CSS Modules]
- **Content Management**: Manual or Markdown-based (this vault)

## Data Flow
1. **Tokens** (`01/tokens`) → Processed into CSS/Tailwind variables.
2. **Patterns** (`02/`) → Applied to UI components, templates and pages
3. **Content** (`05/`) → Injected into pages manually or mentioned source.
